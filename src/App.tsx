function App() {
  return (
    <div style={{ fontFamily: "Arial, sans-serif", maxWidth: 800, margin: "40px auto", padding: "0 16px" }}>
      <h1>KMR Billing Project</h1>
      <p>This repository now focuses on the invoice billing application.</p>
      <p>
        Run the billing app from the <code>website</code> folder using <code>npm install</code> and{" "}
        <code>npm run dev</code>.
      </p>
    </div>
  );
}

export default App;
